# Railway injects DATABASE_URL when Postgres is linked.
DATABASE_URL=postgresql://user:***@localhost:5432/ascend
SESSION_SECRET=generate-with-openssl-rand-hex-32
# First admin email (optional). That user becomes admin on register/login if role still agent.
BOOTSTRAP_ADMIN_EMAIL=
PORT=8080
